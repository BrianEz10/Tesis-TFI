# /etc/fail2ban/filter.d/soc-lab-manual.conf
# Filtro laxo: el bloqueo real lo decide n8n tras la aprobación humana;
# Fail2ban solo materializa el ban cuando n8n ejecuta banip.

[Definition]
failregex = ^.*<HOST>.*$
ignoreregex =
